# Every minute. A deploy is therefore live within ~60s of CI pushing the image, which
# is faster than anyone can walk to the demo machine.
[Unit]
Description=Check for a new SDOC image every minute

[Timer]
OnBootSec=2min
OnUnitActiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
