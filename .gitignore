# secrets — never commit
.env
*.pem
*.key

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# pipeline output
submission.json
*.log

# out/ is scratch, EXCEPT the ablation artifact: it is evidence, it took real money to
# produce, and a judge should be able to read it without running anything.
out/*
!out/ablation.md
!out/ablation.json

# local run artifacts
.venv/
run-*.json

# the runtime LLM switch and spend ledger — local state, not source
.sdoc-state.json

# cached AI answers — reproducible, and can contain document text
.llm-cache/

# organizer answer key — must never land in this repo
ground_truth.json

# os / editors
.DS_Store
.idea/
.vscode/
