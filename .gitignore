# secrets — never commit
.env
*.pem
*.key

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# pipeline output
submission.json
out/
*.log

# local run artifacts
.venv/
run-*.json

# the runtime LLM switch — a local/deployment setting, not source
.sdoc-state.json

# organizer answer key — must never land in this repo
ground_truth.json

# os / editors
.DS_Store
.idea/
.vscode/
